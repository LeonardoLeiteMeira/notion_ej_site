import Contact from "@/components/contact";

export default function Error(){
    return (
        <main className="container">
            <h1 className="section-title">Ops, ocorreu um erro com o seu link</h1>

            <p>Entre em contato comigo que resolvemos!</p>
            <Contact/>
        </main>
    );
}